(function(){
    'use strict';
    var controlerId = NewsCtrl;
    angular.module("bds").controller(controlerId,function ($rootScope, $http, $scope, $state, HouseService, NgMap, $window) {
        var vm = this;
        initNews();
        function initNews() {
            
        }
    })
})